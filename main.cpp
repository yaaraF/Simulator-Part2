#include <iostream>
#include <unistd.h>
#include "Test.h"
//#include "CheckTemplete.h"

int main() {
  vector<int> a ;
  a.push_back(1);
  a.push_back(2);
   //CheckTemplete<vector<int>> *c = new CheckTemplete<vector<int>>(a);
  // c->getCheck();

    return 0;
}